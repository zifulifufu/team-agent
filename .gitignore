.venv/
__pycache__/
*.pyc
.pytest_cache/
node_modules/
desktop/dist/
*.db

# --- Local machine artifacts ---
.DS_Store
*.log
desktop/.vite/
desktop/index.html.bak

# --- Local dev scripts and artifacts (local debugging only, not part of the public repo) ---
_probe/
_sync/
_backup-before-0.4.0/
Claude outputs/

# --- Agent working notes (absolute paths / environment details) ---
.workbuddy/
# The application's own data directory: chats, API-key references, memory. It normally lives in
# the home directory, but `TEAM_AGENT_DATA` can point it inside the tree (tests and smoke runs do
# exactly that), and that is a directory that must never be committed by accident.
.team-agent/
